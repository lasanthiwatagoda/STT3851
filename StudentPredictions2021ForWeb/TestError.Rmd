---
title: "Test Error Project 2 - Leader Board"
author: ""
date: '`r format(Sys.time(), "%B %d, %Y")`'
output: html_document
---

```{r, label = "SETUP", echo = FALSE, results= 'hide', message = FALSE, warning = FALSE}
set.seed(389)
library(knitr)
knitr::opts_chunk$set(comment = NA, fig.show = 'as.is', fig.height = 4, fig.width = 4, prompt = TRUE, highlight = TRUE, tidy = FALSE, warning = FALSE, message = FALSE, tidy.opts=list(blank = TRUE, width.cutoff= 75, cache = TRUE))
```


```{r, echo = FALSE}
APRICE <- read.csv("price.csv")
#########################################################################
#
#Doe_John <- read.csv("Doe_John.csv")
#DoeJohn <- sqrt(mean((Doe_John$x - APRICE$price)^2))
#names(DoeJohn) <- "DoeJohn"
#


### You should at least beat what I gave to start with
yourlastname_yourfirstname <- read.csv("yourlastname_yourfirstname.csv")
BeatThis <- sqrt(mean((yourlastname_yourfirstname$x - APRICE$price)^2))
names(BeatThis) <- "BeatThis"

#
wheeler_chuck <- read.csv("wheeler_chuck.csv")
WheelerChuck <- sqrt(mean((wheeler_chuck$x - APRICE$price)^2))
names(WheelerChuck) <- "WheelerChuck"

#
Swicegood_Jackson <- read.csv("Swicegood_Jackson.csv")
SwicegoodJackson <- sqrt(mean((Swicegood_Jackson$s1 - APRICE$price)^2))
names(SwicegoodJackson) <- "SwicegoodJackson"

#
Hall_Brian <- read.csv("Hall_Brian.csv")
HallBrian <- sqrt(mean((Hall_Brian$x - APRICE$price)^2))
names(HallBrian) <- "HallBrian"

#
LeBlanc_Jacob <- read.csv("LeBlanc_Jacob.csv")
LeBlancJacob <- sqrt(mean((LeBlanc_Jacob$x - APRICE$price)^2))
names(LeBlancJacob) <- "LeBlancJacob"

#
CandelarioJose_Cindy <- read.csv("CandelarioJose_Cindy.csv")
CandelarioJoseCindy <- sqrt(mean((CandelarioJose_Cindy$x - APRICE$price)^2))
names(CandelarioJoseCindy) <- "CandelarioJoseCindy"

#
Jordan_Caitlin <- read.csv("Jordan_Caitlin.csv")
JordanCaitlin <- sqrt(mean((Jordan_Caitlin$s1 - APRICE$price)^2))
names(JordanCaitlin) <- "JordanCaitlin"

```


```{r, echo = FALSE}
Leader <- data.frame(SMSPE = c(BeatThis, CandelarioJoseCindy, LeBlancJacob, HallBrian, SwicegoodJackson, WheelerChuck, JordanCaitlin))
```

```{r, echo = FALSE}
#Leader <- data.frame(SMSPE = c(BeatThis, AllenWill, CampbellMax, VandenbergAndrew, DubeWill))
```

```{r, echo = FALSE}
knitr::kable(Leader[order(Leader$SMSPE), , drop = FALSE], col.names = "$\\sqrt{MSPE}$", align = "l")
```