---
title: "Test Error Project 2 - Leader Board"
author: ""
date: '`r format(Sys.time(), "%B %d, %Y")`'
output: html_document
---

```{r, label = "SETUP", echo = FALSE, results= 'hide', message = FALSE, warning = FALSE}
set.seed(389)
library(knitr)
knitr::opts_chunk$set(comment = NA, fig.show = 'as.is', fig.height = 4, fig.width = 4, prompt = TRUE, highlight = TRUE, tidy = FALSE, warning = FALSE, message = FALSE, tidy.opts=list(blank = TRUE, width.cutoff= 75, cache = TRUE))
```


```{r, echo = FALSE}
APRICE <- read.csv("price.csv")
#########################################################################
#
#Doe_John <- read.csv("Doe_John.csv")
#DoeJohn <- sqrt(mean((Doe_John$x - APRICE$price)^2))
#names(DoeJohn) <- "DoeJohn"
#


### You should at least beat what I gave to start with
yourlastname_yourfirstname <- read.csv("yourlastname_yourfirstname.csv")
BeatThis <- sqrt(mean((yourlastname_yourfirstname$x - APRICE$price)^2))
names(BeatThis) <- "BeatThis"

#
Campbell_Sally <- read.csv("Campbell_Sally.csv")
CampbellSally <- sqrt(mean((Campbell_Sally$x - APRICE$price)^2))
names(CampbellSally) <- "CampbellSally"

#
Holden_John <- read.csv("Holden_John.csv")
HoldenJohn <- sqrt(mean((Holden_John$x - APRICE$price)^2))
names(HoldenJohn) <- "HoldenJohn"

#
Bullman_Haley <- read.csv("Bullman_Haley.csv")
BullmanHaley <- sqrt(mean((Bullman_Haley$x - APRICE$price)^2))
names(BullmanHaley) <- "BullmanHaley"

#
Cave_Tyler <- read.csv("Cave_Tyler.csv")
CaveTyler <- sqrt(mean((Cave_Tyler$s1 - APRICE$price)^2))
names(CaveTyler) <- "CaveTyler"

#
Jones_Breanna <- read.csv("Jones_Breanna.csv")
JonesBreanna <- sqrt(mean((Jones_Breanna$x - APRICE$price)^2))
names(JonesBreanna) <- "JonesBreanna"

#
Seevers_Joseph <- read.csv("Seevers_Joseph.csv")
SeeversJoseph <- sqrt(mean((Seevers_Joseph$x - APRICE$price)^2))
names(SeeversJoseph) <- "SeeversJoseph"

#
Burns_Sean <- read.csv("Burns_Sean.csv")
BurnsSean <- sqrt(mean((Burns_Sean$x - APRICE$price)^2))
names(BurnsSean) <- "BurnsSean"

#
Wheeler_Michael <- read.csv("Wheeler_Michael.csv")
WheelerMichael <- sqrt(mean((Wheeler_Michael$s1 - APRICE$price)^2))
names(WheelerMichael) <- "WheelerMichael"

#
Foote_Lily <- read.csv("Foote_Lily.csv")
FooteLily <- sqrt(mean((Foote_Lily$x - APRICE$price)^2))
names(FooteLily) <- "FooteLily"

#
Lopez_Monica <- read.csv("Lopez_Monica.csv")
LopezMonica <- sqrt(mean((Lopez_Monica$x - APRICE$price)^2))
names(LopezMonica) <- "LopezMonica"

#
Faruq_Leo <- read.csv("Faruq_Leo.csv")
FaruqLeo <- sqrt(mean((Faruq_Leo$x - APRICE$price)^2))
names(FaruqLeo) <- "FaruqLeo"
```


```{r, echo = FALSE}
Leader <- data.frame(SMSPE = c(BeatThis, CampbellSally, HoldenJohn, BullmanHaley, CaveTyler, JonesBreanna, SeeversJoseph, BurnsSean, WheelerMichael, FooteLily, LopezMonica, FaruqLeo))
```

```{r, echo = FALSE}
#Leader <- data.frame(SMSPE = c(BeatThis, AllenWill, CampbellMax, VandenbergAndrew, DubeWill))
```

```{r, echo = FALSE}
knitr::kable(Leader[order(Leader$SMSPE), , drop = FALSE], col.names = "$\\sqrt{MSPE}$", align = "l")
```